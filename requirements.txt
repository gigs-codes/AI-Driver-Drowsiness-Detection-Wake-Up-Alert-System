# ─────────────────────────────────────────────
#  DrowsGuard — Python requirements
#  Install with:  pip install -r requirements.txt
# ─────────────────────────────────────────────

# Core CV / ML
opencv-python>=4.9.0
mediapipe>=0.10.9
numpy>=1.24.0

# Audio alerts
pygame>=2.5.2          # preferred (supports looping)
playsound>=1.3.0       # fallback (simpler, no looping)

# Offline voice TTS
pyttsx3>=2.90

# Live dashboard
matplotlib>=3.8.0

# Optional — remote notifications
twilio>=9.0.0          # SMS/WhatsApp via Twilio
requests>=2.31.0       # Telegram bot API

# Development / testing
pytest>=8.0.0
pytest-cov>=5.0.0
